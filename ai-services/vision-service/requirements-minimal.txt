# AI4Thai Vision Service - Minimal Requirements for CI
# Reduced size build for deployment artifacts

# Web Framework (minimal versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Machine Learning - Core only (smallest possible)
torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cpu
transformers==4.39.3
ultralytics==8.1.47

# Image Processing (minimal)
Pillow==10.1.0
opencv-python-headless==4.8.1.78
numpy==1.24.4

# HTTP and API (minimal)
httpx==0.25.2
aiofiles==23.2.1

# Configuration (minimal)
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Logging (minimal)
structlog==23.2.0

# Utilities (essential only)
psutil==5.9.6

# Production server
gunicorn==21.2.0

# GPU libraries (CPU-only versions for smaller size)
nvidia-ml-py3==7.352.0
