# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      # Rust formatting
      - id: rustfmt
        name: rustfmt
        entry: cargo fmt
        language: system
        files: \.rs$
        args: ["--all", "--", "--check"]
        
      # Rust linting
      - id: clippy
        name: clippy
        entry: cargo clippy
        language: system
        files: \.rs$
        args: ["--workspace", "--all-targets", "--all-features", "--", "-D", "warnings"]
        
      # Cargo fix for automatic fixes
      - id: cargo-fix
        name: cargo-fix
        entry: cargo fix
        language: system
        files: \.rs$
        args: ["--workspace", "--all-targets", "--all-features", "--allow-dirty", "--allow-staged"]
        

  # Additional hooks for other file types
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: mixed-line-ending
